<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Allow cleartext HTTP for local network access to the Pi.
         The Pi may be at 10.42.0.1 (hotspot) or any LAN IP (mDNS). -->
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>
